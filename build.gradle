plugins {
  id 'java'
}

repositories {
  mavenCentral()
}

jar {
  manifest {
    attributes "Main-Class": "fourwins.GameMain"
  }
  }

dependencies {
  testImplementation 'org.junit.jupiter:junit-jupiter:5.9.0'
}

test {
  useJUnitPlatform()
  testLogging.showStandardStreams(true)
}